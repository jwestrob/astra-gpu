# Upstream references

This directory holds local, reproducibly fetched upstream sources, builds,
and installations. Generated contents are ignored by Git.

No source from GPL-licensed GPU-HMMER implementations may be copied into the
project. Prior implementations are architectural and historical references
only unless their license is explicitly compatible and provenance is recorded.
